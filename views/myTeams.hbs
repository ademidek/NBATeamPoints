<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Teams</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>
<body class="bg-gray-100">
    <div class="absolute top-0 left-0 p-4">
        <a href="javascript:history.back()">
            <img src="/img/backarrow.png" alt="Back" style="width: 40px; height: 30px;" />
        </a>
    </div>

    <div class="absolute top-0 right-0 p-4">
        <a href="/dashboard">
            <img src="/img/home.png" alt="Home" style="width: 30px; height: 30px;" />
        </a>
    </div>

    <div class="container mx-auto px-4 py-8">
        <h1 class="text-gray-600 text-6xl text-center mb-8">My Teams</h1>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {{#each teams}}
                <div class="bg-white shadow-lg rounded-lg p-4 flex flex-col items-center">
                    <h3 class="text-2xl font-semibold mb-4 text-center">{{this.teamName}}</h3>
                    <div class="flex space-x-4">
                        <a href="/teamDetails/{{this._id}}" class="bg-yellow-500 hover:bg-yellow-600 text-white font-bold py-2 px-4 rounded transition duration-200 ease-in-out text-sm">View Roster</a>
                        <form action="/deleteTeam/{{this._id}}" method="POST" onsubmit="return confirm('Are you sure you want to delete this team?');">
                            <button type="submit" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded transition duration-200 ease-in-out text-sm">Delete Team</button>
                        </form>
                    </div>
                </div>
            {{else}}
                <div class="col-span-1 md:col-span-2 lg:col-span-3">
                    <p class="text-center text-gray-600">No teams created yet.</p>
                </div>
            {{/each}}
        </div>
    </div>
</body>
</html>
